apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: default
spec:
  type: NodePort
  selector:
    app: keycloak
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080 # Port corrigé dans la plage valide